# 풀이 지도: 핵심에 추론·계산·마무리를 붙이는 제작 흐름

## 판단

원자는 고정된 단계가 아니다. 예를 들어 그래프의 오르내림을 구하는 일은 한 문항에서는 핵심 추론이고, 다른 문항에서는 앞서 찾은 관계를 계산으로 확인하는 작업이다. 따라서 원본의 수학적 적용 조건은 유지하고, **이번 설계에서 맡길 역할**을 따로 저장한다.

사용자는 핵심 노드를 고른 뒤 추론·계산·마무리 중 붙이고 싶은 역할을 고른다. 추천은 선택한 노드의 결과를 사용하는 재료만 보여 준다. 필요한 중간 연결의 이름과 추가 작업을 보고 붙인다. 마지막 질문을 고르면 필요한 남은 연결을 함께 담는다. 서로 다른 가지의 정보를 함께 쓰는 노드에는 여러 선이 들어온다.

## 구현

- `composition-graph.js`: 기존 `requires / provides`의 함수·관계 대상·가정 범위를 맞춰 실제 정보 전달 선을 구성한다. 시작 조건을 늘리지 않고 실행 가능한 순서를 찾는다. 주어진 조건과 도출한 정보를 구분한다.
- `mindmap-ui.js`: 핵심을 가운데 두고 앞에서 필요한 생각과 뒤에 붙이는 생각을 펼친다. 위치는 드래그·방향키·방향 버튼으로 바꾼다. 위치와 수학적 연결은 별개다. 축소·확대·전체 보기·자동 배치를 제공한다.
- 각 노드의 역할은 `reasoning / calculation / finishing`. 기본 추천 역할과 이번 문항의 역할을 분리한다. 역할 이름 변경은 실제 계산 작업 수치나 난도를 바꾸지 않는다.
- 자동 저장·구성 파일·공개 화면에서 연결 도우미로 이동할 때 `mapState`의 역할과 위치를 유지한다. 이전 구성은 기본 역할로 표시하고, 기존 재료·주문·목표를 보존한다.
- 지도 편집은 최대 25회 연속 되돌릴 수 있다. 이전 목록과 조건 편집은 보조 메뉴로 접었다.
- 제작 요청의 `design_intent.graph`는 역할과 전달 정보가 붙은 연결을 담는다. 화면 좌표는 모델에 전달하지 않는다. 서버는 그래프를 다시 구성하여 잘못 그린 선·누락·다른 대상의 정보를 거절한다. 반환 결과도 선택한 연결을 보존했는지 검사한다.
- 새로운 연결 도우미는 `reasoning_graph_version: 1`을 알린다. 이전 도우미가 지도 의도를 버린 채 제작하지 않도록 화면에서 버전을 확인한다.

## 회귀검증과 실제 제작

기존 검사와 그래프 검사 7개를 합쳐 54개 테스트를 통과했다. 병렬 정보의 합류, 입력 없는 순환, 함수·범위 불일치, 없는 선 추가, 필수 선 누락, 역할 변경, 중간 연결 추천, API/CLI 요청 보존을 검사한다. 임시 표본은 메모리 또는 정리되는 임시 디렉터리만 사용한다.

브라우저에서는 `합성방정식 핵심 → 계산 가지(연결 포함) → 교점 개수로 마무리`를 실제로 조작했다. 여러 선이 마지막 계산에 합쳐지고 제작 버튼이 활성화됐다. 세 번의 되돌리기로 이전 사용자 구성·추가 주문·난도 설정을 복구했다. 노드 역할 변경과 위치 이동도 별도로 검증했다.

Codex의 실제 Sol 세션에서도 `F(x)=x(x−2)²`, `F(x−F(x))=0` 문항을 제작했다. 고정된 높이 두 개를 얻는 가지와 `H=F−x`의 그래프를 분석하는 가지가 마지막 개수 판단에 합쳐진다. 반환 결과는 구조 검사를 통과했고, 서로 다른 실근 6개를 원래 합성식에 대입하고 전체 실근 수를 별도 기호 계산으로 검산했다. 실제 결과는 비공개 제작 기록에 보관한다.

## 품질 판단과 현재 범위

연결이 타당하다고 좋은 문항이 되는 것은 아니다. 실제 확인 문항도 인수분해로 그래프 분석을 우회할 수 있다. 이는 구조적 연결 검사만으로 각 가지의 수학적 필수성을 보장할 수 없다는 실제 사례다. 핵심 조건을 뺐을 때의 변화, 더 쉬운 풀이, 장식처럼 남은 계산을 제작 결과에서 검토해야 한다.

현재는 등록된 46개 계약 안에서 추천한다. 없는 연결을 임의로 선으로 그려 승인하지 않는다. 동일한 원자를 서로 다른 대상에 여러 번 배치하려면 기존 `by: operation_id` 출처를 인스턴스 식별자로 확장해야 하므로, 현 지도는 한 구성에서 각 원자를 한 번만 사용한다. 추천 역할은 현재 자산에 대한 초기값이며 실제 문항의 역할은 사용자가 바꿀 수 있다.

다음 평가 지표는 노드 수가 아니라 **쓸 만한 문항을 얻는 시간, 교사의 수정량, 우회 풀이로 사라진 핵심 비율, 실제 풀이에서 측정한 계산·추론 부담**이다. 축적된 문항에 대한 이 평가를 바탕으로 추천 순위와 작업량 추정을 보정한다.
