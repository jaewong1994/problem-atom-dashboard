# 문항 제작 구조 검토와 실행 계획

## 판단

기존 구조는 자산 보존과 연결 조건 검사에는 쓸 수 있지만, 비전문 개발자가 좋은 문항을 완성하는 최종 구조로는 부족했다. 모든 원자에 같은 비중을 두고 순방향으로 붙이는 것보다 **핵심 생각 → 마지막 질문 → 필요한 풀이 흐름 → 계산·추론 목표 → Sol 제작 → 검토**가 목적에 맞다. 이번 변경은 이 흐름과 검증 경계를 구현한다. 최적의 난도 모델이나 무한 제작의 품질을 입증한 것은 아니다.

## 확인한 문제와 이번 구현

| 기존 문제 | 변경 | 확인 방법 |
|---|---|---|
| 최종 질문 없이 붙는 재료를 추천 | 현재 핵심에서 도달할 수 있는 질문을 고르고 해당 질문의 흐름 제안 | 세 시작 구성에서 교점 개수·정수 선별·이동거리까지 도달 |
| 가교 뒤 일반 풀이를 한 번 더 붙여야 하는 경로가 보이지 않음 | 일반 단계 두 차례 확장, 각 확장에 최대 세 가교 탐색 | 도함수 복원 → 그래프 → 위치 → 거리 정의 → 거리 계산 |
| 핵심 지정이 자유 서술문에만 존재 | 핵심 단계·대상·가정 범위, 목표 정보, 계산·추론 수준을 `design_intent`로 전달 | 브라우저 구성, 세션 이전, 서버 재구성, CLI 전달, 결과 대조 |
| 결론을 시작 조건에 주고 재료를 형식적으로 나열해도 통과 | 목표를 도출했는지와 핵심 출력부터 목표까지의 의존 관계 검사 | 실제 기존 검사 우회를 재현하고 새 요청에서 거부 |
| 연결되기만 하면 답과 무관한 재료도 허용 | 목표의 선행 정보로 이어지지 않는 단계 표시·제거·되돌리기 | 독립된 다른 함수의 재료를 넣으면 준비 완료 불가 |
| 마지막 단계의 출력에서 생기는 충돌을 놓침 | 단계 실행 전 출력까지 포함해 충돌 검사 | 충돌한 출력은 남기지 않음 |
| 46개 재료를 전부 펼친 화면 | 핵심·질문·흐름을 기본 화면에 두고 전체 목록은 접기 | 데스크톱·모바일·키보드 확인 |
| 순서만 틀린 구성을 사람이 고쳐야 함 | 기존 조건으로 가능한 순서 정리 | 없는 가정을 추가하지 않고 순서만 정리 |

## 책임 분리

```text
통합 자산·출처·연결 계약
        ↓
selection-model: 재료 선택·핵심 틀·직접 연결 탐색
        ↓
composition-planner: 마지막 질문·완성 경로·남는 재료 검사
        ↓
model-contract: 구조화 제작 목표 + 원자 + 발문 자료
        ↓
Codex 로그인 Sol: 실제 수식·조건·문항·풀이 생성
        ↓
연결 검사 + 핵심/목표 보존 검사
        ↓
독립 수학 검산 → 교사 품질 판단 → 난도 보정 자료 축적
```

연결 검사는 식의 의미나 정답 유일성을 증명하지 않는다. 목표가 `integer_answer`로 기록됐다고 문항의 한국어 질문과 실제 답이 일치하는 것까지 보장하지 않는다. 이 점은 검토 화면에 계속 표시한다.

기존 원본 107개·46개 연결 단계·9개 가교는 삭제하거나 임의 승인하지 않는다. 기존 v1 요청과 결과는 열 수 있다. 새 제작실은 구조화 목표를 사용하며, 구형 연결 도우미가 목표를 조용히 버리지 않도록 버전 확인 후 제작한다. 질문이 없던 기존 저장 구성에는 목표를 임의로 붙이지 않는다. 핵심 틀을 바꿔도 추가 주문은 보존하므로 제작 전에 두 내용의 일치를 확인한다.

## 선택한 절충

- 별도 데이터베이스나 프레임워크 전환 없이 순수 JavaScript 설계 모듈을 추가했다. 정적 배포와 기존 세션 연결을 유지하고 같은 검사를 브라우저·서버에서 사용한다.
- 추천은 추측 점수보다 검증 가능한 의존 관계를 우선한다. 현재 일반 단계 확장은 두 차례, 상위 탐색은 24개 상태, 직접 연결 탐색은 128개 상태로 제한한다. 범위 밖 경로가 없다는 뜻은 아니다.
- 대표 질문 이름은 현재 자료에 맞춘 목록이다. 새 종류의 최종 질문에는 표시 이름을 추가해야 한다. 기존 결과 종류로 연결되는 새 자산은 공통 탐색을 그대로 사용한다.
- UI의 점진적 공개에 딱 맞는 스킬 검색 근거는 찾지 못했다. 화면 복잡도 축소·명시적 라벨·44px 조작 크기 등 기본 UX 원칙과 실제 화면 확인으로 판단했다.
- ‘단순·심화·킬러’는 설계 목표다. 원자 수·작업 단위를 학생 난도 점수로 환산하지 않는다.

## 다음 고도화의 우선순위와 통과 기준

1. **독립 검산 범위 확대**: 모델의 자체 점검과 다른 풀이/계산으로 존재·유일성·경계값·경우 누락을 확인한다. 지원하지 않는 수식 유형은 보류하며 자동 승인하지 않는다.
2. **핵심의 실제 기여 검토**: 구조뿐 아니라 핵심 조건 제거와 더 쉬운 우회 풀이를 사람이 확인한다. 단계가 많아졌는데 핵심 사고가 불필요해진 문항은 실패 사례로 축적한다.
3. **난도와 생산성 보정**: 목표별 여러 핵심 유형으로 제작하고 교사 수정 시간, 승인률, 실제 풀이 시간, 핵심 발견의 어려움을 기록한다. 단순 계산 증가와 추론 심화를 분리해서 비교한다. 현재 정량 개선률은 측정하지 않았다.
4. **자산 계약 보강**: 실패 사례에서 드러난 미지수·정의역·관계·최종 질문의 정보를 원자에 환류한다. 정답을 억지로 통과시키려고 시작 가정을 늘리지 않는다.

이후 자동 독립 검산을 추가할 때는 ‘검산 완료’의 기준과 실패 상태부터 정하고 실행한다. 새로운 LLM이 같은 풀이를 반복하는 것만으로 독립 검증을 대신하지 않는다.

## 이번 확인 결과

- 전체 자동 회귀검증 47개 통과. 목표 경로, 결론 선제 제공 차단, 대상·가정 보존, 무관한 단계 검출, 순서 복원, 마지막 출력 충돌, CLI로 설계 목표 전달을 새로 검사했다.
- 실제 Sol 로그인 실행 1건에서 `design_intent`가 전달됐고 결과가 형식·연결·목표 보존 검사를 통과했다. 함수의 도함수가 `(x−1)(x−√2)`이고 양의 가중치가 `t²+1`인 기존 검산 사례를 재제작한 회귀 확인이다. 정답은 1이다. 새로운 유형의 품질 향상이나 난도 보정 실적으로 세지 않는다.
- 실제 반환 문항의 식을 확인한 뒤 기존 독립 기호 검산과 대조했다. 양수 인수, 매개변수의 세 위치 관계와 두 겹침 경우, 실수 후보 `1, √2`와 정수 후보 `1`을 확인했다. 자유 서술된 조건 역할 중 일부 문장은 과도하게 일반화될 수 있으므로 사람 승인으로 처리하지 않고 계속 검토 대기로 둔다.
- 실제 제작 기록은 개인 `.pa-session`에 보관하고 공개 배포에서는 제외한다. 회귀용 가상 데이터는 테스트 메모리에만 만들었다.
