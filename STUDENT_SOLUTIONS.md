# 학생이 혼자 읽을 수 있는 해설

2026-09-21 · `PA-STUDENT-SOLUTION-001/v1`

`model-contract.js`의 `SOLUTION_GUIDANCE`를 공통 제작 프롬프트에 포함한다. Codex 구독, Sol API, Gemini·ChatGPT·Claude 웹 인계 모두 같은 기준을 받는다. 한글 출력기는 완성된 `solution`을 조판하며, 내려받을 때 별도의 모델 호출로 해설을 바꾸지 않는다. 변경은 새로 제작하는 결과부터 적용한다.

## 옵시디언에서 참고한 기준

- `ngd2/문제은행/12_해설_골든셋_숫자변형.md`: 관계식을 세운 뒤 대입, 분기·제약의 이유, 우연한 지름길 지양, 학생용 본문과 기계용 기록 분리.
- `ngd2/문제은행/2026-09-01_작업일지.md` 및 `91_결정로그.md` D-085: 골든셋 v6의 완성본·자기완결·간결성·길이 예산.
- 위 문서가 가리키는 `공장/골든셋_프롬프트_v6_빌드.py`와 `회귀자료/해설작성_규격_v2_숫자변형친화_20260816.md`를 대조했다. 본문에는 온톨로지 프로파일과 제작 메타데이터를 노출하지 않는다.

## 이번 제작실에 적용한 방식

첫 문장에서 출발 조건과 풀이 방향을 밝히고, 핵심 전환마다 조건→판단의 이유→필요한 식을 연결한다. 분기를 나누는 기준, 빠진 경우가 없는 이유, 후보가 탈락한 조건을 남긴다. 반복 계산은 대표 한 번으로 묶고, 단순 산술로 문단을 늘리지 않는다. 어려운 표현을 풀어 써도 수학적 의미를 바꾸지 않는다. 특히 함수가 0이 되는 위치와 부호가 바뀌는 위치를 혼동하지 않는다.

예를 들어 ‘판별식이 0이다’로 건너뛰지 않고, 실제로 서로 다른 실근이 하나여야 하는 상황이라면 ‘이 이차방정식의 서로 다른 실근이 하나여야 하므로 중근을 갖는다. 따라서 판별식이 0이다’로 연결한다.

v6의 글자 수 예산은 이 제작실에서 **압축을 위한 참고 기준**으로 적용한다. 긴 복합 문항의 필수 근거를 자동으로 잘라내거나, 길이만으로 정상 해설을 반려하지 않는다. 탐색·자기 수정·제작 평가는 `self_checks`와 `condition_roles`로 분리하되, 후보의 원래 조건 대입처럼 답을 확정하는 확인은 학생 해설에 남긴다.

## 확인 범위

기존 회귀검증에 API·Codex CLI 실제 전달 프롬프트·세 웹 인계의 공통 기준 포함 여부와 서버 버전 노출 검사를 추가했다. 이는 프롬프트 전달 경로 검사이며, 모든 생성 해설의 교육적 품질이 검증됐다는 뜻은 아니다. 생성 결과의 논리 누락과 수학적 정답 검토는 유지한다. 기존 완료 결과는 자동 재작성하지 않는다.
