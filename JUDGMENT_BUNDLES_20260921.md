# 판단 묶음으로 문항 구성하기

제작실의 기본 입구를 세 가지 미적분 핵심 예제에서 **판단 묶음 또는 낱개 재료 선택**으로 바꿨다. 사용자는 특정 낱개를 지배적인 핵심으로 지정하지 않아도 된다. 묶음은 여러 판단·계산·가교가 함께 수행하는 역할을 뜻하며, 새 원자나 승인 자산으로 승격하지 않는다.

## 제공하는 흐름

1. 묶음 7개 또는 현재 등록된 낱개 46개에서 이름·예시로 찾는다. 한 번에 펼치는 낱개 수는 8개로 제한한다. 혼자 시작하면 마지막 질문이 없는 중간 재료는 시작 버튼 대신 함께 쓸 묶음을 안내한다.
2. 묶음을 누르면 문제에 더해지는 성격, 내부 판단, 예시, 필요한 시작 정보와 사용 조건을 확인한다.
3. 빈 구성에 담으면 계약에서 필요한 시작 정보를 준비한다. 진행 중인 구성에는 기존 정보만으로 이어지는 경우에만 붙인다. 다른 구성으로 새로 시작하는 동작은 별도로 표시한다.
4. 지도에서는 묶음을 한 덩어리로 표시한다. 내부 판단을 펼쳐 수정하거나 묶음 전체를 뺄 수 있다. 삭제·추가·질문 변경은 기존 되돌리기를 사용한다.
5. 질문을 정하고 남은 연결을 붙여 AI 제작으로 넘긴다. 계산과 판단의 작업 목록을 구분하되, 묶음 크기를 난도로 표시하지 않는다.

## 데이터와 연결

`judgment-bundles.js`의 각 정의는 기존 operation ID의 `members`, 직관적인 이름, 역할 설명, 예시를 가진다. 입력·출력·사용 금지 조건·출처는 기존 연산 계약에서 유지한다. 온톨로지의 전략/복합 요소에 해당하는 제작용 계층이며 외부 정본에는 쓰지 않는다.

- 구성에 같은 ID가 있다는 이유만으로 묶지 않는다. 적용 대상·관계 대상·가정 범위가 같고 내부의 실제 정보 전달이 확인되어야 한다.
- 묶음 결과를 미리 주어진 조건으로 만들어 내부 판단을 우회하면 묶음으로 인식하지 않는다.
- 일부 구성만 이미 있을 때는 빠진 재료만 추가한다. 관계 대상과 범위를 포함해 입력을 맞추며, 조건을 조용히 보충하지 않는다. 이미 사용한 ID를 다른 대상에 중복 배치하지 않는다.
- 외부 단계를 거쳐 다시 묶음 내부로 들어오는 구조는 접지 않는다. 접은 지도에서도 외부 연결을 유지하고, 펼치면 원래 그래프가 그대로 나온다.
- 저장 파일에는 원래 노드와 계약을 보존한다. 묶음은 다시 계산하므로 기존 구성·제작 기록·한글 출력 묶음과 호환된다.

새 묶음은 `members`를 등록하고 모든 내부 판단이 실제로 연결되며 최종 답에 기여하는 회귀 사례를 통과시킨다. 다른 과목을 추가할 때는 그 과목의 연산·입출력·예시를 먼저 등록한다. 표시하는 구현 과목은 내부 연산의 공통 과목으로 계산한다. 일반적인 이름만 붙여 수학Ⅰ·확률과 통계 전체 제작이 지원된 것처럼 취급하지 않는다. 현재 자산 범위는 수학Ⅱ·미적분Ⅰ 중심이다.

## 모델 전달과 호환성

`knowledge.authoring.bundles`를 클라이언트·API·Codex·웹 AI 요청에 포함한다. 서버는 이 정보를 현재 계약에서 다시 계산하므로 임의의 묶음 메타데이터를 신뢰하지 않는다. 내부 `members`와 `joins`를 보존하고, 각 판단 제거·우회 풀이·후보 선별의 실효성을 점검하도록 요청한다.

기존 `design_intent.core` 필드는 연결 추적을 위한 호환 필드로 유지한다. 새 화면의 요청은 `core_role: connection_anchor`를 명시하여 그 낱개만 가장 어려운 판단으로 만들라는 요구를 없앴다. 역할 표시가 없는 과거 요청의 의미는 유지한다. 연결 도우미는 `judgment_bundle_version: 1`을 제공하며, 예전 도우미에서는 재시작 안내를 표시한다.

## 검증

- 전체 회귀 76개 통과: 7개 묶음의 연결·최종 질문 기여, 낱개 시작, 일부 묶음 보완, 다른 대상·범위·관계의 차단, 중복 방지, 조건 추가 없음, 지도 접기/펼치기, 저장 복원, canonical API/웹 요청의 묶음 보존을 포함한다.
- 기존 세션·결과 검증·한글 미주 출력 회귀를 유지했다. 실제 AI 문항 품질이나 난도 보정이 입증되었다는 의미는 아니다.
- 실제 화면에서 묶음 담기 → 질문 선택 → 제작 가능, 내부 펼치기/접기, 묶음 삭제/복구, 낱개 검색을 확인했다. 390×844에서 가로 넘침 없음, 콘솔 오류 없음. 기존 사용자의 구성·추가 주문·한글 출력 묶음을 복원했다.
- UI/UX 스킬 검색은 묶음 펼침에 맞는 데이터베이스 항목을 찾지 못했다. 기존 디자인 토큰과 일반적인 점진적 공개, 44px 조작 영역, 텍스트 설명, reduced-motion 규칙을 적용했다.

이 작업은 고정된 완성 문항 템플릿을 늘리는 변경이 아니다. 현재는 검토한 일곱 조합의 묶음 정의를 지원하며 사용자가 임의의 묶음을 저장하는 편집기, 한 연산의 다중 인스턴스, 전 과목 자산 확장은 별도 개발 범위다.
