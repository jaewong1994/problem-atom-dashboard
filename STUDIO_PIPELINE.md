# 제작실 v2 운영

`studio.html`이 새 진입점이다. 2차 세미나 7개 자료 → 6개 구조 → 21개 후보 요소를 사용한다. `composition-catalog.json`은 원문별 해시, 쪽수, 두 분석의 동일 문항 연결, 재도출 과정, 보완 내용과 원자 계약을 담는다. 원문 PDF/HWP, 사용자 경로, 원문 이미지 자체는 배포하지 않는다.

## 실행과 검사

```powershell
python -m pip install sympy==1.14.0
python -m unittest test_studio -v
python prepare_pages.py --build
```

Node 22와 Python을 사용한다. 브라우저의 `studio-engine.js`를 Node에서 그대로 실행한 뒤 Python/SymPy가 별도로 식을 만들어 미분·적분·실근·경계값을 대조한다. 6개 구조 × 3단계 × 2가지 계산 크기 × 10개 번호 = 360개 새 문항이다. 기존 192개 문항 검사도 함께 실행한다. 테스트 실패 시 Pages 빌드를 중단한다. 집계 결과와 생성기 해시는 `studio-validation.json`에 남긴다. 테스트 문항은 메모리에서 생성되며 승인 자산에 넣지 않는다.

## 구조별 변경

| 구조 | 기본 | 표준 | 심화 |
|---|---|---|---|
| 절댓값·교점 | 두 조각을 알려 줌 | 절댓값을 직접 제거 | 높이가 정수의 제곱일 때 허용값 찾기 |
| 끊김 메우기 | 근의 모양을 알려 줌 | 연속 조건으로 모양 결정 | 근의 순서를 이용한 가중 합 |
| 구간 이동 | 관계식 상수를 알려 줌 | 끝점 값·기울기로 결정 | 앞 구간을 재사용해 두 칸 뒤 적분 |
| 움직이는 구간 | 근의 간격을 알려 줌 | 개수함수 극한에서 간격 추론 | 복원한 함수에 절댓값·교점 계산 연결 |
| 부호가 다른 적분 | 구간별 함수를 알려 줌 | 적분과 연속성으로 함수 복원 | 교점 개수 조건으로 기울기의 정수합 |
| 이동거리 | 방향전환 시각을 알려 줌 | 속도로 방향전환 시각 결정 | 속도 0 조건으로 숨은 계수부터 복원 |

‘계산 크기’는 수치와 계수를 바꾼다. 예상 계산은 의도한 풀이의 미분·방정식·대입 등 큰 작업 단위의 횟수다. 사칙연산 개수나 모든 가능한 풀이의 최단 길이는 아니다. 기본/표준/심화는 같은 구조 안의 설계 목표이며, 여섯 구조의 난도를 서로 동등하게 보정한 등급이 아니다.

## 추가 원자와 호환성

`studio-engine.js`의 families에 설명과 출처를 등록하고 실제 생성 분기를 만든다. 원자를 단순히 많이 선택할수록 어려워지는 방식이 아니다. 입력·출력·전제가 맞는 연결만 구현한다. 표시되는 `assets`에는 해당 판본에서 실제 사용하는 원자만 포함한다. 후보 ID를 정본 ID로 바꾸거나 자동 승격하지 않는다.

새 구조는 다음을 함께 추가해야 한다.

1. 학생용 문항에 포함되는 조건과 정답 유일성.
2. 원문 재도출, 적용 범위, 실패 사례, 접속하는 원자의 입력·출력 근거.
3. `test_studio.py`의 독립 수학 검사와 경계값·조건 제거 증인.
4. 선택을 바꿀 때 문항과 필요한 풀이가 실제 바뀌는 검사.
5. 원자 출처·표시 수식·재현·잘못된 입력 거절 검사.

검사가 지원되지 않는 임의 원자 조합은 UI에 제공하지 않는다. 이 버전은 유한한 수치 범위의 여섯 생성 규칙이다. 반복 생성은 가능하지만 고유 수치 조합을 소진할 수 있으며, 이때 구조나 계산 크기를 바꾸도록 안내한다. 같은 문장 중복 제외는 서로 다른 발상이나 참신성을 보장하지 않는다.

## 저장과 변경

문항과 풀이를 JSON으로 저장한다. 같은 버전·설정·번호로 재현한다. 보관함은 브라우저에 최대 200개 저장하며 자동 서버 공유는 아니다. 보관 해제는 번호로 재생성 가능하다. 학생용 인쇄에는 풀이와 설정을 포함하지 않는다. 생성기 수학 변경 시 VERSION 및 HTML의 파일 버전을 함께 갱신하고 기존 저장 판본은 별도로 보존해야 한다.

## 표현 규칙

공적시험 어휘 통계와 grammar-lock v2를 참조한다. `구하시오`, `에 대하여`, `서로 다른`, `최댓값` 같은 표현을 사용한다. UI의 원자 이름은 쉬운 행동 설명이고, 학생용 문항은 모호하지 않은 수학 표현을 유지한다. 통계 파일의 해시와 적용 범위는 catalog.language에 기록되어 있다. 난도 판정·교육과정 채택·인적 승격 상태를 어휘 빈도로 정하지 않는다.
