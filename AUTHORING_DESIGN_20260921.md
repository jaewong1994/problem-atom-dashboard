# 판단과 질문을 중심으로 한 문항 제작실

## 확인한 문제

풀이 지도는 연결 조건을 보여 줬지만, 선택한 노드 다음에 추론·계산·마무리라는 큰 분류를 열었다. `rolesFor()`는 모든 재료를 추론 후보로 허용했다. 사용자는 각 재료의 목적과 마지막 질문에의 기여를 스스로 판단해야 했고, 지도·전체 자산·시작 조건 사이를 오갔다. 이전 화면의 위계 개선만으로 해결되지 않았다.

## 이번 흐름

**핵심 생각 → 마지막 질문 → 필요한 연결 → 문항 제작·검토**로 기본 선택의 자유도를 줄인다. 지도는 실제 정보 의존 관계를 보여 주고 어느 노드든 눌러 되돌아갈 수 있다. 핵심 틀, 질문, 다음 재료, 시작 조건은 같은 작업대의 편집 패널에서 다룬다. 전체 자산과 대상 이름 수정은 상세 편집으로 남긴다.

- 다음 추천은 현재 조건으로 실행 가능하고, 현재 핵심에서 선택한 질문까지의 경로에 실제로 포함되어야 한다. 선택한 가지와의 관계도 확인한다.
- 각 추천은 발동 신호, 학생의 판단, 실제 행위, 수학 예시, 사용할 정보, 추가할 작업을 제시한다. 역할 이름을 고르게 하여 모든 원자를 펼치지 않는다.
- 후보 전달용 가교 03·05는 소비하는 풀이와 묶어서 선택한다. 원래 계약·출처·ID는 유지한다.
- 불필요한 재료와 부족한 조건을 명시한다. 질문을 바꿔도 기존 재료를 자동 삭제하지 않는다. 삭제·복구·시작 조건 체크 변경은 되돌릴 수 있다.
- 노드 수나 기존 초기 작업 단위를 난도/예상 시간으로 표시하지 않는다. 계산할 일과 판단할 일을 문장으로 미리 보여 준다.
- 새 노드 등장에는 240ms의 opacity/transform 전환을 쓰고, 선택한 연결을 강조한다. 계속 움직이는 장식 애니메이션을 두지 않으며 reduced-motion을 따른다.

## 온톨로지 교훈 반영

검토한 원천 문서와 SHA-256은 `authoring-lessons.json`에 기록한다. 자동 승인되지 않은 외부 온톨로지 노드를 수입하지 않았다. 46개 PA 연산의 제작 설명을 원래 ID에 덧붙였다. `signal → decision → action → outputs`는 제작용 안내이며 정본 온톨로지 분류의 승격이 아니다.

`sync_authoring_lessons.py --vault <Obsidian Vault>`는 원천 변경을 읽기 전용으로 감지한다. 변경이 있으면 교훈·영향 범위·회귀 사례를 재검토한 후 판본과 해시를 갱신한다. CI는 로컬 Vault 없이도 모든 등록 연산의 제작 설명, 연결 선택, 전달 계약을 검사한다. 새 연산에 제작 설명이 없으면 기본 추천에 노출하지 않고 커버리지 검사가 실패한다.

`model-contract.js`는 요청마다 `knowledge.authoring`을 정본 코드에서 재구성한다. 실제 Codex·API·AI 웹 요청에 핵심, 판단, 입력 출처, 결과, 조건 결합, 조건 제거/우회 풀이 검토를 함께 전달한다. 브라우저가 보낸 임의 제작 설명을 서버 정책으로 신뢰하지 않는다. 결과 JSON과 기존 저장 구성은 그대로 호환된다.

## 검증 범위와 남은 한계

회귀는 핵심 3종의 도달 가능한 6개 질문에 대해 하나씩 붙이기와 일괄 완성을 검사한다. 잘못된 함수·범위·충돌·무관한 가지·조작된 추천·중복 추가를 거부한다. 기존 수학·모델·세션·한글 미주 출력 회귀도 유지한다.

현재 기본 시작 틀은 3개이며, 전체 46개가 모두 단독 시작 틀을 가진 것은 아니다. 복잡한 조합의 완전 탐색은 보장하지 않는다. 한 원자의 다중 인스턴스는 기존 엔진에서 지원하지 않는다. 선택지 제한과 실제 화면 사용성은 개선했지만 문항 품질, 난도 보정, 교사 수정 시간 단축은 별도 제작/사용자 평가가 필요하다. 조건 그래프를 통과해도 쉬운 다른 풀이로 핵심을 우회할 수 있으므로 독립 검산과 교사 검토는 유지한다.

## 확인 결과

- 자동 회귀 70개 통과. 이번 목적별 선택·온톨로지 안내 전달 회귀 7개를 포함한다. CI에서는 기존 한글 조판 키트 전용 4개가 환경에 따라 건너뛰어진다.
- 실제 화면에서 핵심 선택 → 교점 질문 → 필요한 3개 연결을 하나씩 추가 → 제작 가능까지 확인했다. 전체 자산 목록은 열지 않았다. 시작 조건 해제 시 제작 차단, 되돌리기로 복원, 전체 해제·복원도 확인했다.
- 390×844 화면에서 편집 패널 우선 표시, 지도 펼침/접힘, 노드 선택 후 편집 복귀와 가로 넘침 없음을 확인했다. 브라우저 콘솔 오류 0. 기존 사용자의 다섯 재료·여섯 조건·추가 주문과 한글 출력 묶음을 보존했다.
- 새 제작 설명을 실제 Sol 로그인 요청에 전달하여 「극값 하나를 남기는 정수」를 제작했다. 연결 검사가 통과했고 정답 1을 독립 기호 계산으로 확인했다. 도함수는 `(x−1)(x−√2)(x−a)(x²+ax+a²+3)`이며 마지막 인수는 항상 양수다. 따라서 실수 후보 `1, √2` 중 정수 `1`만 남는다. 이미 알고 있는 수학 구조를 이용한 전달 회귀이며 새로운 난도·품질 성과로 세지 않는다.
- 실제 제작 기록과 독립 검산 근거는 개인 `.pa-session`에 보관한다. 회귀용 가상 문항은 메모리에서만 생성하며 자산에 추가하지 않았다.
