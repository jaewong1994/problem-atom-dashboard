# 박스 안에서 예시 펼치기

## 사용자 흐름

문제은행3의 카드 펼침과 선택 분리 방식을 참고했다. 닫힌 박스에는 이름과 구체적인 문제 조건을 보여 주고, 박스를 누르면 같은 자리에서 풀이 단계와 결과가 펼쳐진다. 담기는 별도 버튼으로 실행한다. 목록으로 돌아가기 위해 화면을 전환할 필요가 없다.

- 네이티브 details/summary를 사용해 키보드로도 펼치고 접을 수 있다.
- 펼친 내용에는 짧은 등장 애니메이션을 적용하고 움직임 줄이기 설정을 따른다.
- 묶음의 구성 판단, 세미나 원문 표현, 사용 조건은 박스 안에서 추가로 펼친다.
- 선택한 범위와 연결 가능성 판단은 기존 제작 엔진을 그대로 사용한다.

## 이름과 예시

`box-examples.js`에 낱개 재료 46개와 판단 묶음 7개의 예시를 둔다. 모든 예시는 실제 식 또는 수치가 있는 조건, 두 단계 이상의 풀이, 결과로 구성한다. 선택한 노드와 다음 연결 추천에서도 같은 구체적 예시를 사용한다.

세미나 1·2회차 기록의 개념 상자와 풀이에서 확인한 이름 또는 표현 28개를 표시 이름에 반영했다. 예: ‘연속 함수는 혼자두고 분석하자’, ‘우미분계수 + 좌미분계수 분해’, ‘피적분함수의 평행이동과 적분구간의 이동’. 표시 이름을 다듬었을 때도 작성자·문항·쪽/개념 번호와 원래 표현을 함께 남긴다. 나머지는 출처를 만들어 붙이지 않고 이해를 위한 예시로 구분한다.

확인 자료: seminar-01-review-20260913의 원문 페이지 이미지와 review-data.json, seminar-02-20260920의 source-1~7.txt와 manifest. 원문에 적용 범위가 생략되거나 부정확한 표현은 그대로 일반 명제로 쓰지 않고 예시와 사용 조건에서 범위를 명시했다.

예시는 표시용 데이터다. 원자 식별자, 연결 계약, 시작 정보, 생성 요청을 변경하지 않는다. 예시의 상수와 식도 제작 조건으로 자동 삽입하지 않는다. 이름 검색은 세미나 표현, 이전 이름, 예시의 식과 풀이를 모두 포함한다.

## 검증

- `prepare_pages.py --build`: 86개 테스트 통과.
- 새 회귀검증: 모든 재료/묶음의 예시 누락 검사와 KaTeX 렌더링, 표시 데이터 변경 전후 생성 요청 일치, 합성 방정식의 해·접점·이동 적분·극값 및 경계값 독립 계산.
- 로컬 브라우저: 제자리 펼침/접기, Enter 접기, 세미나 이름 검색, 박스 담기 후 마지막 질문 진입, 되돌리기로 기존 구성 복구 확인.
- 기본 화면 및 폭 390px 모바일 검수. 가로 넘침과 수식 렌더 오류 없음. 브라우저 오류 로그 없음.
- 검수 후 기존 재료 5개·시작 정보 6개·마지막 질문 미선택·추가 요구 606자·출력 묶음 1문항을 유지했다. 실제 AI 제작 호출은 실행하지 않았다.
