# Codex 로그인으로 문항 제작

`세션제작_열기.cmd`를 실행하면 연결 도우미가 백그라운드에서 켜지고 제작실이 열립니다. Codex CLI에 ChatGPT 계정으로 로그인되어 있어야 합니다. 별도 API 키를 입력하지 않으며, Codex 이용 한도를 사용합니다. 기본 제작 모델은 `gpt-5.6-sol`입니다. 다른 모델로 자동 전환하거나 실패한 요청을 자동 재시도하지 않습니다.

공개 사이트에서는 재료를 선택한 뒤 `Codex 연결` → `선택한 재료로 연결 화면 열기`를 누르세요. 선택한 재료와 목표가 이 PC의 연결 도우미 화면으로 넘어갑니다. PC가 꺼져 있거나 연결 도우미가 실행되지 않으면 세션 제작을 사용할 수 없습니다. 브라우저만으로 Codex 계정 정보를 읽는 방식이 아닙니다.

## 제작 흐름

1. 위의 핵심 틀로 시작하거나, 재료 카드의 `핵심으로`를 눌러 문항의 중심을 정합니다. 핵심 틀은 필요한 시작 조건과 핵심까지의 풀이 재료를 함께 담습니다. 기존 선택을 바꾸며 `되돌리기`로 복구할 수 있습니다.
2. `핵심에 이어 붙일 재료`에서 이어 쓰는 정보와 추가 작업량을 보고 담습니다. 필요한 중간 재료도 표시하며 함께 추가합니다. 왼쪽 분류나 검색으로 다른 재료를 직접 고를 수도 있습니다.
3. 오른쪽에서 풀이 순서와 연결을 확인합니다. 부족한 조건과 이어 주는 요소는 제안으로 표시하며, 사용자가 추가해야 반영합니다. `전체 선택 해제`는 필터 밖 재료·시작 조건·핵심 지정까지 비웁니다. 다음 구성 변경 또는 새로고침 전에는 `되돌리기`로 복구하며, 추가 주문·계산 부담·추론 목표·제작 기록은 유지합니다.
4. 계산 부담과 목표 추론 난도(단순·심화·킬러)를 각각 고릅니다. 재료의 작업량은 계획 단계의 예상이며 실제 난도 측정값은 아닙니다.
5. `Codex로 1문항 제작`을 누르면 Sol이 문항·정답·풀이·조건별 역할을 반환합니다. 새로고침해도 진행 중인 제작과 완료 기록을 다시 확인할 수 있습니다.

로그인된 계정은 공유하지만 현재 Codex 대화의 전체 이력을 자동으로 이어받지는 않습니다. 매번 선택한 원자, 등록된 연결 규칙, 발문 참고 자료, 목표를 전달하는 임시 제작 실행입니다. 기존 대화에서 직접 제작하려면 `요청·결과 파일로 주고받기`의 요청 복사를 사용할 수 있습니다.

핵심 지정은 자동 저장, 구성 파일, 연결 도우미 이동에 유지됩니다. 제작 요청에는 핵심 재료의 이름·ID와 ‘핵심을 빼면 어떤 판단이 불가능해지는가’를 설명하라는 주문을 넣습니다. 이는 설계 지시이며 핵심의 실제 기여가 자동 검증됐다는 뜻은 아닙니다.

## 이어 붙일 재료 추천 범위

추천은 현재 계획이 연결 조건을 만족할 때 작동합니다. 핵심 결과를 직접 또는 중간 재료를 거쳐 사용하는 후보만 제시합니다. 핵심을 지정하지 않았다면 선택한 재료의 결과를 기준으로 찾습니다. 함수 이름·관계의 대상·가정 범위를 일치시키고 필수 조건·금지 조건·중복 결과를 검사합니다. 없는 가정을 추가하지 않습니다.

최대 3개의 중간 재료, 128개 탐색 상태 안에서 찾으며 사용하지 않는 우회 단계는 제거합니다. 추천에 필요한 중간 재료를 별도 선택지로 중복 표시하지 않습니다. 클릭 시 현재 구성으로 다시 검사하고 담습니다. 등록된 규칙 안에서 연결할 수 있다는 뜻으로, 교육적 자연스러움·풀이 시간·문항 존재와 정답 유일성·실제 학생 난도를 보증하지 않습니다. 추천이 없다는 것도 모든 조합이 불가능하다는 뜻은 아닙니다.

## 결과 검토와 보관

결과는 요청 번호·자산 판본·선택 재료 누락·금지 연결·필수 조건을 검사합니다. 통과 표시는 **형식과 연결 검사**이며, 모델 자체 풀이를 독립 검산이나 사람 승인으로 취급하지 않습니다. 아직 검토 전인 문항은 자동으로 공유 자산에 등록하지 않습니다.

이 PC의 요청과 결과는 `.pa-session`에 저장하며 Git과 Pages 배포에서 제외합니다. 임시 CLI 출력 폴더는 실행 종료 후 삭제합니다. 원문 자산을 변경하지 않습니다. 도우미는 `127.0.0.1:8987`에서만 실행하고 같은 출처와 일회성 실행 토큰으로 요청을 확인합니다. 인증 정보는 읽거나 웹으로 전달하지 않습니다. CLI는 읽기 전용·도구 비활성·임시 모드로 실행합니다.

새 빌드 반영 후에는 기존 도우미 프로세스를 종료하고 다시 실행해야 새 자산 판본을 읽습니다. `.pa-session/service.pid`는 이 실행의 PID입니다. 종료 전 해당 PID가 이 프로젝트의 `server/session-service.cjs`인지 확인하세요.

## 회귀검증

`python prepare_pages.py --build`는 기존 수학·연결 검사와 박스 선택·세션 접근·중복 방지·취소·재시작 복구·CLI 임시 파일 정리를 검사합니다. 자동 테스트의 모델 응답은 메모리/임시 폴더의 표본이며, 유료 호출이나 실제 Codex 호출을 하지 않습니다. 실제 세션 제작 확인은 별도로 수행해야 합니다.

공식 근거: [Codex 인증](https://developers.openai.com/codex/auth), [비대화형 실행](https://developers.openai.com/codex/noninteractive).

## 2026-09-20 실제 세션 확인

로그인된 Codex에서 Sol로 두 차례 제작했습니다. 첫 결과는 수학적 풀이와 별개로 원자 ID에 설명을 붙이고 대상 식별자를 바꿔 연결 검사에서 탈락했습니다. 출력 스키마에 등록 ID 목록을 넣고, 연결 기록과 풀이 설명의 역할을 구분한 뒤 같은 다섯 재료로 다시 제작했습니다. 두 번째 결과는 형식·연결 검사를 통과했습니다. 실패 결과는 통과 기록으로 바꾸지 않았습니다.

통과 문항은 `f′(x)=(x−1)(x−√2)`와 양의 가중치를 둔 차형 적분을 이용했습니다. 매개변수의 실수 후보는 `1, √2`이고 정수 후보는 `1`입니다. `test_session_math.py`에서 원래 적분을 직접 계산해 도함수를 비교하고, 항상 양수인 추가 인수를 완전제곱으로 확인하고, 후보 위치의 모든 순서 및 겹침 경우에서 실제 부호 변화를 독립 검사했습니다. 정답은 1입니다.

이는 세션 연결과 한 조합의 수학적 성립을 확인한 실적입니다. 다양한 난도의 문항 품질이 보정됐다는 뜻은 아닙니다. 화면은 계속 독립 검산·사람 검토 대기를 기본 상태로 유지하며, 교사 승인은 별도입니다.

## 핵심 조건을 고정한 5문항 실험

이후 첫 통과 문항을 포함해 총 5문항을 같은 제작실과 Sol 세션에서 만들었습니다. 고정한 조건은 ‘극값을 갖는 위치가 정확히 하나’이며, 적분을 미분해 부호 변화를 비교하는 공통 구조를 유지했습니다. 정보를 직접 주기, 식을 미분하기, 같은 함숫값으로 함수 복원하기, 극값 조건을 거꾸로 읽기, 정수 후보 개수로 경우 나누기로 설계 주문을 바꿨습니다. 설계 주문은 이번 작업에서 별도로 구체화했고 난도는 상대적인 예상입니다.

정답은 차례로 1, 2, −5, −27, 58입니다. 다섯 문항의 원래 적분과 도함수, 실제 부호 변화, 해의 누락 여부를 별도 기호 계산으로 확인했습니다. 3~5번은 처음에 미등록 가교가 보고돼 연결 검사에서 보류됐습니다. 이를 통과로 덮어쓰지 않고 새 가교 08·09를 증명·등록한 뒤 검토본만 재검사했습니다. 5번의 제어문자로 손상된 수식 표기 한 곳도 표시용 사본에서 수정했습니다.

가교 08은 삼차함수의 같은 높이 두 점에서 도함수의 두 근 존재를 보장합니다. 가교 09는 정수합이 0이 아닌 경우, 도함수의 두 근 중 정수인 것이 한 개인지 두 개인지 나눠 함수 후보를 복원합니다. 허용값이 어느 함수의 근인지 관계로 기록하여 다른 함수에서 얻은 후보가 섞이지 않게 했습니다. 두 가교는 AI 후보이며 사람 승인이 아닙니다.

핵심 묶음은 풀이의 성격을 강하게 유지했지만, 1→2번의 차이는 주로 계산 준비였습니다. 더 높은 추론 부담은 정보 복원·역추론·경우 분류에서 생겼습니다. 이 결과만으로 단일 원자의 보편적 영향력이나 학생 난도를 보정할 수는 없습니다.

로컬 비교 자료는 상위 프로젝트의 `output/core-atom-difficulty-20260920`에 보관합니다. `export_difficulty_experiment.py`는 실제 완료 기록 5개와 독립 검산 표시, 연결 미통과 사유를 요구하며 원본과 검토본을 함께 보존합니다. 이 개인 제작 기록은 Pages에 배포하지 않습니다.
