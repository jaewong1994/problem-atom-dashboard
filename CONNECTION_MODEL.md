# 원자 연결 모델 v1

이전 문항 제작실은 폐기했다. 기존·신규 자산을 공통 연결 규칙으로 통합하고, **모델이 문항을 제작하며 연결 엔진은 제약을 검사**하는 구조로 바꾼다. 현재 제작은 **Codex의 ChatGPT 로그인 + gpt-5.6-sol**을 사용한다. API 키가 필요하지 않으며, 실패 시 자동 재시도하거나 Astra로 전환하지 않는다. 현재 대화 전체 이력 대신 선택한 원자와 목표를 전달한다. 고정된 여섯 유형의 로컬 생성기는 재사용하지 않는다.

## 하나의 자산 목록, 서로 다른 역할

원본 107개를 `connection-registry.json`에 통합했다. 원자 40개, 1차 풀이 묶음 13개, 2차 원자 21개, 조건 카드 19개, 문제 골격 4개, 원문 조합 10개다. 107개 모두를 똑같은 조립 부품으로 취급하지 않는다.

| 자료 | 통합 후 역할 |
|---|---|
| 개념·스킬·판단 | 실행 단계의 근거, 일부는 직접 연결 단계 |
| 전략·문제 유형·원문 조합 | 풀이 전체의 참고·시작점 |
| 조건 카드 | 문제에서 줄 정보를 작성하는 표현 후보 |
| 골격 | 미지수와 조건이 놓일 자리의 참고 |
| 연결 단계 | 필요한 정보 → 수행할 일 → 얻을 정보 |
| 가교 | 대상·표현·질문을 바꾸어 다른 단계로 전달 |

원본 ID, 출처, 검토 상태와 내용은 보존한다. 40개 원자가 13개 묶음과 연결되는 것은 병합이나 사람 승인과 다르다. 오래된 설명의 과도한 일반화는 원본 기록에 남기되 실행 계약에는 제한된 범위를 적는다. 실행 계약이 없는 기록은 `reference_only`로 표시하며 실행하지 않는다. 외부 온톨로지에 자동 승격하거나 덮어쓰지 않는다.

현재 연결 계약 46개 중 가교는 9개다. ‘정해진 높이에서 교점 개수 구하기’와 ‘요구 개수를 만드는 높이 범위 구하기’는 같은 자산을 활용하지만 입력·출력이 달라 별도 실행 단계로 분리했다.

## 원자 속성

각 단계에는 다음을 저장한다.

- 사람이 읽을 쉬운 이름과 안정적인 ID, 판본.
- `requires`: 필요한 정보 종류와 적용 대상.
- `provides`: 다음 단계로 건넬 정보 종류와 적용 대상.
- `forbids`: 있으면 사용할 수 없는 조건.
- `guard_note`: 수학적 적용 범위와 실제 계산에서 확인할 내용.
- `supports`: 어느 원자·분석 기록을 근거로 만들었는지.
- `kind`: 일반 풀이 단계인지 가교인지.
- `influence`: 표현을 바꾸기 / 후보를 줄이기 / 다른 풀이로 이어 주기. 난도 점수가 아니다.
- `work`: 잠정적인 식 계산·경우 나누기 작업 단위. 중복 단계를 다시 넣으면 더하지 않는다.
- 검토 상태, 교육과정, 별도 문항 검산 필요 표시.

입력과 출력은 이름이 비슷하다는 이유로 연결하지 않는다. `{type, subject, object?, scope}`가 일치해야 한다. `subject`는 같은 매개변수 설정과 정의역을 갖는 수학적 대상의 이름이고, `scope`는 같은 가정이 적용되는 범위다. 관계를 다루는 `object`는 ‘a가 어느 함수의 높이인지’를 구분한다. F에서 H=F−x/m를 만들면 H에 대한 결과를 F의 결과로 되돌려 쓰지 않는다. ㄱ의 가정도 ㄴ으로 넘기지 않는다.

현재 대상의 식·수치·구간 자체를 컴퓨터 대수계로 해석하지는 않는다. ‘다항식 차수가 2 이상’ 같은 시작 정보는 설계자가 선언한다. 이 선언이 실제 식에 맞는지는 다음 문항 검산 층의 책임이다. 따라서 이 엔진의 통과 표시는 수학적 증명 완료나 정답 유일성을 의미하지 않는다.

## 연결 판정

1. **연결 조건 충족**: 필요한 정보가 같은 대상·가정 범위에 모두 있고 금지 조건이 없다.
2. **가교를 거치면 연결**: 현재 정보로 수행 가능한 가교를 최대 3단계까지 탐색했다. 중간에 가정을 새로 만들어 넣지 않는다.
3. **조건 부족**: 무엇이 부족한지 목록을 보여 준다. 미지원 관계를 가능으로 간주하지 않는다.
4. **연결 금지**: 금지 사실과 충돌하거나, 대상 이름이 빠졌거나, 변환 전후 함수를 같은 대상으로 지정했다.

자산 둘의 출력·입력 유형이 겹치는 것은 검색 후보일 뿐이다. 실제 순서 전체를 검사하기 전에는 ‘바로 연결 가능’이라고 승인하지 않는다. 실패한 단계는 결과를 남기지 않는다. 각 도출 정보는 사용한 계약 판본·대상 지정·선행 조건 목록을 기록한다.

## 아홉 가교

| 가교 | 연결 | 반드시 필요한 조건 |
|---|---|---|
| 기울어진 직선을 수평선으로 바꾸기 | 1차 합성방정식 → 2차 교점 세기 | m≠0, 외부 근의 값, F의 식과 차수. H=F−x/m로 별도 대상 생성 |
| 다항식에서 증감과 높이 구하기 | 확정된 함수 → 교점·거리 | 상수가 아닌 다항식. 미분·근·높이 계산이 새로 필요 |
| 유한한 답 후보를 정수 선택에 넘기기 | 1차 극값 조건 → 2차 정수 선택 | 후보 목록을 유지. 최소·최대 사이를 채워 넣지 않음 |
| 도함수와 한 점의 값으로 함수 되찾기 | 1차 함수 복원 생각 → 2차 거리 | 다항식 도함수와 기준 함숫값. 현 계약은 0이 아닌 도함수로 제한 |
| 허용 높이를 매개변수로 옮기기 | 교점 범위 → 정수 선택 | 매개변수가 높이 자체임을 명시. a² 등은 지원하지 않음 |
| 근의 위치와 반복 횟수 함께 구하기 | 함수의 식 → 절댓값·근 합집합 | 0인 다항식 제외. 위치만으로 반복 횟수를 추측하지 않음 |
| 그래프를 시간에 따른 위치로 읽기 | 증감·높이 → 이동거리 | 미분가능성, 시간 범위, 범위 내 방향전환 |
| 같은 높이의 두 점에서 기울기 변화 확인하기 | 삼차함수의 같은 함숫값 → 도함수의 서로 다른 두 근 | 서로 다른 두 입력, 0이 아닌 최고차항 계수. 근의 수치는 별도 계산 |
| 정수 후보가 한 개인지 두 개인지 나눠 거꾸로 찾기 | 정수합 → 미정인 함수 후보 | 두 근과 최고차항 계수가 고정된 삼차함수. 허용값이 같은 함수의 도함수 두 근과 일치한다는 관계. 알려진 합이 0이 아님. 후보 재검산 필요 |

가교를 넣으면 다른 문제의 모습으로 바꿀 수 있지만, 그만큼 계산이 추가되거나 원래 추론을 가릴 수도 있다. 원자를 많이 넣는 것을 난도 증가로 보지 않는다. 구조가 필요한 이유를 설명하지 못하는 가교는 채택하지 않는 것이 다음 생성기의 기준이다.

## 실제 식으로 확인한 세 연결

- F=x(x−2)²에서 F(x−F(x))=0을 H=F−x의 높이 0, −2 문제로 바꾸면 근 6개다.
- F=x³−9x²+15x, Gₐ=∫ₐˣ(F(x)−F(t))(t²+1)dt의 극값이 하나인 정수 a는 1, 5다.
- X′=3t²−12t+9, X(0)=0, 0≤t≤4에서 위치를 복원하고 방향을 나누면 이동거리는 12다.

이는 고정 검산 사례다. 화면에서 임의로 편집한 연결의 생성 결과로 표시하지 않는다.

## 새 자료 반입

`connection-incoming/*.json`은 앞으로 들어오는 자료의 누적 입력이다. 기존 문항 유형별 생성 분기를 새로 짤 필요 없이, 기존 정보 종류를 쓰는 새 단계는 통합 직후 예전 자산과 검색·검사된다.

1. 원본 ID·쉬운 이름·종류·출처를 `records`로 넣는다.
2. 실행할 내용만 `operations`로 적는다. 입력·출력·금지 조건·작업량·근거가 필요하다.
3. 기존 정보 종류로 표현할 수 없으면 새 종류와 검사를 먼저 추가한다. 알 수 없는 종류를 비슷한 이름으로 자동 연결하지 않는다.
4. 반입 전 필수 항목, ID 충돌, 출처, 자기 순환, 자동 승인 시도를 검사한다.
5. 전체 목록을 다시 만들고 모든 기존 회귀검증을 수행한다.

```powershell
python build_connections.py --import 새자료.json
python prepare_pages.py --build
```

반입 파일의 형식은 `connection-extension.schema.json`을 참고한다. 레코드만 들어온 경우 목록에는 보존되지만 연결 단계로 실행되지 않는다. 테스트용 자료는 메모리에서만 생성하며 반입 폴더에 남기지 않는다. 기존 ID의 내용 변경은 덮어쓰기로 처리하지 않고 별도의 판본 검토가 필요하다.

현재 한 단계에서 다룰 대상 자리는 `$f`, `$h`, `$a`다. 다른 자리가 필요한 구조는 화면과 모델 반환 계약을 함께 확장한 뒤 반입한다. 자산 판본은 원본 JSON의 정규화한 내용으로 계산하므로 Windows와 Linux의 줄바꿈 차이 때문에 제작 요청이 무효화되지 않는다.

기존 1차·2차 입력을 갱신할 때는 원본 파일을 수정한 뒤 `python build_connections.py --refresh-sources`로 다시 통합한다. `--refresh-sources`는 로컬 Obsidian의 조건 카드·골격을 읽는다. CI는 저장된 `connection-design-sources.json`을 사용한다. 원문 PDF·HWP·로컬 경로는 배포하지 않는다.

## 회귀검증과 제작실 재설계 순서

현재 검사는 모든 계약의 조건 하나씩 제거, 금지 입력, 다른 대상·보기 범위, 가교 탐색, 순서와 중복, 미등록 자산, 판본 불일치, 새 자료 반입, 3개 실제 식 사례, 기울기 변환 60조합을 포함한다. 전체 쌍 검색은 연결 후보 검색의 안전성을 확인하는 검사이며 전체 수학 조합의 성립 검증이 아니다. 이전 생성 사례 192개 검산도 참고 자산의 회귀검사로 유지한다.

모델 제작과 검산은 다음 순서를 따른다. 요청·반환·연결 검사까지 구현했으며, 임의 문항의 독립 수학 검산 자동화는 아직 남아 있다.

1. 이 연결 설계로 **풀이 경로**를 먼저 만든다.
2. 각 단계의 대상을 실제 식·매개변수·정의역에 묶는다. 이름만 붙이는 현재 설계를 수식 모델로 구체화한다.
3. 조건을 연립해 해가 존재하는지 확인하고, 묻는 값이 유일한지 독립 검산한다.
4. 각 조건을 빼서 정답이나 풀이가 변하는지, 우회 풀이가 핵심을 없애는지 확인한다.
5. 계산 부담과 추론 부담을 분리해 기록한다. 반복 출제·교사 판단·학생 풀이 데이터로 보정한다.
6. 공적시험 발문 규칙으로 문제를 작성하고 검산한 풀이와 함께 사람에게 검토시킨다.

`33_발문어휘통계_공적시험.md`와 온톨로지 참조는 통합 목록에 유지한다. 발문 빈도가 수학적 연결 가능성이나 난도를 결정하지 않는다. 현재 단계는 최적 모델이나 무한 문항 생산의 완성이 아니라, 그 전에 필요했던 공통 자산·연결 조건 층이다.

## 이전 제작실 처리

`studio-engine.js`, `studio-ui.js`, `studio.css`, 해당 생성기 테스트와 결과 파일을 삭제했다. Git 이력에서만 복원할 수 있다. `studio.html`은 종료 안내와 기존 브라우저 보관함 내보내기만 제공한다. 이전 한 구조 생성 UI도 `motif-library.html`에서 제거했다. 문항 보관함과 원자 사람 검토 기록은 삭제하지 않았다.

## Codex 로그인 세션 제작 흐름

`선택한 원자 + 제작 목표 → 요청 묶음 → 연결 도우미 → Codex Sol 세션 → 문항·풀이·조건 역할·수정된 풀이 경로 → 연결 검사 → 독립 수학 검산 → 사람 승인`

- `model-contract.js`: 통합 자산·언어 규칙·온톨로지 참조를 포함한 요청과 구조화 결과 계약. 요청 번호와 자산 판본을 맞추고, 미등록 원자·가교·미해결 항목을 검사한다.
- `connections.js`, `selection-model.js`: 문제은행3 형태의 분류·박스 선택·선택 목록. 계산 부담과 추론 요구를 따로 지정하고 결과·조건 역할·기록을 검토한다.
- `session-client.js`, `server/session-service.cjs`: 공개 화면의 선택을 로컬 연결 도우미로 넘기고, 제작 시작·상태·취소·기록을 관리한다.
- `server/codex-session.cjs`: 기존 ChatGPT 로그인으로 공식 Codex CLI를 임시·읽기 전용·도구 비활성 모드에서 실행한다. API 키나 로그인 토큰을 읽지 않는다.
- `server/model-adapter.cjs`: 서버의 최신 자산으로 요청을 다시 구성하고 Responses API에 Sol/high로 1회 요청한다. 클라이언트가 모델이나 지침을 바꿔도 서버 설정을 우회하지 못한다.
- `server/service.cjs`: HTTPS 앞단에 배포할 API 서비스. 키는 서버 환경변수에만 둔다. 사이트에는 서비스 접근 토큰만 일시 입력하며 저장하지 않는다.
- `model-provider.json`: 현재 `codex_chatgpt_session`. API 경로는 대안으로 남아 있으나 현재 화면에서 사용하지 않는다. 세션 제작 운영은 `SESSION_WORKSPACE.md`에 설명한다.

새 가교 제안은 별도 항목으로 반환하며 등록·수학 검토 전에는 통과시키지 않는다. 결과의 `accepted`는 형식·연결 검사 통과이고 `release_ready`는 항상 false다. 모델이 적은 사실이 문항에 실제로 주어졌는지, 조건이 해를 유일하게 하는지까지 이 검사가 증명하지 않는다.

Sol의 충분성은 모델 이름으로 확정하지 않는다. 첫 실호출 평가에서는 기존·신규 연결 3종 × 계산 부담 2단계 × 추론 요구 2단계의 12문항을 같은 요청 판본으로 제작하고 아래 내용을 사람이 독립 채점한다. 목표는 평가 설계이며 현재 달성 실적이 아니다.

1. 조건을 만족하는 해의 존재와 정답의 유일성, 풀이 오류 여부.
2. 고른 원자·가교가 실제로 필요한지, 조건을 뺐을 때 문제가 달라지는지.
3. 추론을 늘렸을 때 단순 계산·장문만 늘어난 것은 아닌지, 쉬운 우회 풀이가 있는지.
4. 승인 문항 수, 수정 횟수, 교사 검토 시간, 실제 토큰 비용.

수학 오류가 있는 문항은 실패다. 같은 오류가 반복되면 해당 원자 계약·프롬프트·검산 항목을 수정하고 같은 요청을 재검증한다. 고가 모델로 덮어 해결하지 않는다. API 운영 방법은 `server/README.md`에 있다.
