# 이전 로컬 계정 서버 참고 자료

2026-09-21부터 실제 공유 계정은 기존 Supabase 프로젝트를 사용합니다. 현재 운영·초대·관리자 기능은 [SUPABASE_ACCOUNTS.md](SUPABASE_ACCOUNTS.md)를 보세요. 아래 내용은 배포에 사용하지 않는 SQLite 대안의 기록입니다.

## 구현 상태와 운영 전환

Supabase 없이 작동하는 Node.js 24 + SQLite 계정 서버를 추가했다. 계정 서버가 같은 주소에서 웹 화면과 `/team/` API를 함께 제공한다. 회원 가입은 없고 김연수·이광훈·김상범·민재웅 네 계정만 등록된다.

**아직 공용 서버로 전환하지 않았다.** 현재 GitHub Pages와 8987 연결 도우미는 기존 공유 저장소를 계속 사용한다. 공용 HTTPS 주소와 영구 저장 공간이 확정되면 아래 순서로 이전한다. GitHub Pages 자체는 비밀번호 검증 API와 SQLite를 실행할 수 없다.

계정 서버는 준비된 `_site`만 공개한다. 계정 DB와 초대 코드는 `.pa-team/`에 두고 Git·웹 배포·Docker 빌드 입력에서 제외한다. 단일 서버 인스턴스에서 운영하며 영구 볼륨을 사용한다.

## 사용자 흐름

1. 홈에서 본인 이름을 고른다.
2. 최초 한 번은 운영자가 전달한 개인 초대 코드와 새 비밀번호를 입력한다. 코드 없이 다른 강사의 비밀번호를 먼저 설정할 수 없다.
3. 이후 이름과 비밀번호로 로그인한다.
4. 매 로그인마다 **AI 구독 연결 / AI 웹 무료 이용**을 고른다. Codex는 각 사용자의 PC에 로그인된 연결 도우미를 사용한다. AI 웹은 요청·답변을 복사하는 기존 방식이며 서비스 자체의 이용 한도를 따른다.
5. 현황판에서 본인이 선점한 문제만 완료·완료 취소를 할 수 있다. 교차분석 참여는 유지하며 다른 강사의 기록을 변경할 수 없다.
6. 검수 기록은 공용 계정 서버에 저장되고 제작실에서 읽는다. 검토자 이름은 로그인 계정에서 정한다.

새 계정 서버의 제작 초안·웹 요청·한글 출력 목록은 브라우저 안에서도 계정별 키로 분리된다. 기존 연결 도우미의 저장한 구성은 유지한다. 계정 인증이 Codex나 Gemini의 구독·로그인을 대신하지 않는다.

## 기존 기록 연결

2026-09-21 공유 현황판의 백업 기능으로 기존 선점·완료 기록을 읽었다. 17행을 네 고정 계정 ID와 현재 문항 ID로 연결했고, 중복 브라우저 기록을 합쳐 15개 소유 기록으로 정리했다. 과거 문항 ID에 포함된 `기하` 등의 표기는 `dashboard-data.json`의 `legacyIds`를 사용해 정식 공통 문항 ID로 연결한다. 단원 이름만 보고 추측하지 않는다.

| 계정 | 소유 기록 | 완료 | 분석 중 |
|---|---:|---:|---:|
| 김연수 | 2 | 1 | 1 |
| 이광훈 | 7 | 6 | 1 |
| 김상범 | 3 | 2 | 1 |
| 민재웅 | 3 | 3 | 0 |

같은 문제의 교차분석이 있어 12개 완료 기록은 고유 문항 9개에 해당한다. `민재웅T`의 과거 JSON 기록도 확인했으며 최신 이벤트가 완료 취소인 항목은 다시 완료로 만들지 않았다. 미확인 이름·문항 ID는 별도 보고하며 자동으로 다른 계정에 배정하지 않는다.

이전은 기본적으로 미리보기다. 명시적으로 `--apply`를 붙일 때만 적용한다. 새 서버에서 사용자가 변경한 기록은 다시 가져오기로 덮어쓰지 않는다. 기존 자료에서 온 기록은 더 최근의 백업으로 갱신할 수 있다. 이전 저장소는 삭제하지 않는다.

```powershell
node server/team-admin.cjs import 이전기록.json
node server/team-admin.cjs import 이전기록.json --apply
node server/team-admin.cjs status
```

## 로컬 확인과 초대 코드

```powershell
python prepare_pages.py --build
node server/team-service.cjs
```

기본 확인 주소는 `http://127.0.0.1:8992/index.html`이다. 원격 공동 운영 주소가 아니다.

```powershell
node server/team-admin.cjs invite 김연수
```

초대 코드는 `.pa-team/초대코드-김연수.txt`에만 저장하며 터미널에는 출력하지 않는다. 7일 동안 유효하고 처음 비밀번호를 설정하면 소진된다. 동일 명령을 다시 실행하면 이전 미사용 코드는 무효가 된다. 이미 비밀번호를 설정한 계정에는 재발급하지 않는다. 비밀번호 초기화 운영 기능은 이번 범위에 포함하지 않았다.

## 공용 서버 배포

`Dockerfile.team`은 빌드와 회귀검증을 수행한 뒤 Node.js 런타임과 정적 화면을 묶는다. Docker 환경 자체에서의 빌드는 아직 실행하지 않았으며 배포 대상에서 검증해야 한다.

- `PA_TEAM_ORIGIN`: 실제 HTTPS 사이트 주소. 예: `https://실제-도메인`.
- `PA_TEAM_BIND`: 프록시 뒤 컨테이너에서는 `0.0.0.0`.
- `PA_TEAM_PORT`: 기본 컨테이너 포트 8080.
- `PA_TEAM_DATA`: 영구 볼륨 경로. 컨테이너는 `/data`.

프록시는 외부 HTTPS를 종료하고 원래 Host를 유지한다. API를 다른 도메인에 따로 노출하지 않는다. 서버가 지정한 Origin·Host·CSRF 값을 검증한다. 비밀번호는 scrypt(N=131072,r=8,p=1)와 개별 salt로 저장하며 세션 쿠키는 HttpOnly, SameSite=Lax, 공개 HTTPS에서 Secure를 사용한다. 쿠키 원문은 DB에 저장하지 않는다. 로그인은 12시간 유효하고 재로그인 시 제작 방식은 미선택으로 시작한다. 로그인 시도 제한과 최초 설정 경합 검사를 적용했다.

전환 순서:

1. 공용 서버·HTTPS·영구 볼륨을 준비한다.
2. 기존 현황판의 **선점·완료 기록 백업**에서 마지막 사본을 확보한다.
3. 이전 보고서의 네 이름·문항·완료 개수를 확인하고 서버의 DB로 반영한다.
4. 네 강사에게 각자의 초대 코드와 새 주소를 전달한다. 공개 GitHub에 초대 코드나 DB를 올리지 않는다.
5. 두 컴퓨터에서 같은 계정 및 다른 계정으로 소유권과 기록 공유를 확인한다.
6. 기존 사이트의 진입 주소를 새 서버로 안내하고 이전 저장소의 쓰기를 중단한다. 필요한 기존 댓글·검수 원장은 별도 확인한다.

백업은 서버를 멈춘 뒤 `.pa-team/` 전체 또는 SQLite의 일관된 백업을 저장한다. 실행 중인 DB 파일 하나만 복사해 WAL의 변경을 놓치지 않도록 한다. 기존 Supabase 댓글 테이블은 별도 보존하며 이번 선점 기록 이전에 포함되지 않는다.

## 검증 범위

회귀검증 96개: 고정 계정만 최초 설정 가능, 초대 코드·비밀번호·시도 제한·세션 만료, 서버 재시작 후 계정·기록·세션 유지, 매 로그인마다 제작 방식 재선택, 본인 소유권, 같은 문제의 교차분석, 소급 매핑·중복 제거·재반영, HttpOnly 쿠키·Origin/CSRF·이름 변조 차단, 검수자 이름 검증, 기존 문항 조합 및 문서 출력 회귀.

격리된 메모리 DB에서 로그인 → 무료 제작 방식 → 기존 완료·선점 표시 → 목록 체크박스 권한 → 제작 방식 반영 → 로그아웃·재로그인을 브라우저로 확인했다. 실제 네 계정의 비밀번호는 설정하지 않았다. 공유 서버 배포와 다중 컴퓨터 통합 검증은 공용 주소 확정 후 수행한다.

설계 참고: [OWASP 비밀번호 저장](https://cheatsheetseries.owasp.org/cheatsheets/Password_Storage_Cheat_Sheet.html), [OWASP 세션 관리](https://cheatsheetseries.owasp.org/cheatsheets/Session_Management_Cheat_Sheet.html).
