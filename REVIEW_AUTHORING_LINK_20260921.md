# 재료 검수와 문항 제작 연결

## 사용 흐름

1. 상단 **재료 검수**에서 검토자 이름을 입력한다.
2. 추천 묶음의 정리문, 원래 항목, 연결된 제작 박스의 예시와 사용 조건을 확인한다.
3. **승인하고 제작실에 연결**을 누르면 승인한 정리문과 출처가 저장된다.
4. **재료 찾기**에서 승인 정리문을 찾거나, 검수 카드의 제작 박스 링크로 이동한다.
5. 제작실에서 **검수 내용이 연결된 재료만 보기**로 걸러 보고, 펼친 박스에서 정리문을 확인한다. 선택한 박스에 연결된 정리문은 실제 AI 제작 요청에도 들어간다.

기존 세미나 추천 묶음 11개에 아직 검수 경로가 없던 제작 박스를 더해 총 42묶음에서 46개 제작 박스를 확인할 수 있다. 묶음 승인은 원본 항목을 물리적으로 합치는 동작이 아니다. 정리문의 승인과 연결 규칙·완성 문항의 수학적 검증은 별개다.

## 변경과 저장

- 승인한 정리문을 수정하면 다시 검수 대기가 된다. 보류한 정리문은 새 제작 요청에 전달하지 않는다.
- 분리한 항목은 각각 정리문을 작성하고 승인한다. 승인된 부분에 포함된 원본 항목과 연결된 박스에만 검수 내용이 전달된다.
- 원본 또는 해당 박스의 연결 조건이 달라지면 재검수가 필요하다. 관계없는 새 자료가 추가됐다는 이유로 기존 승인을 취소하지 않는다.
- 여러 화면에서 동시에 수정하면 저장 판본을 비교해 덮어쓰기를 막는다. 충돌 시 최신 검수를 불러온 뒤 다시 확인한다.
- 제작실은 화면으로 돌아올 때와 실제 제작 직전에 검수 기록을 갱신한다. 서버도 요청에 담긴 검수 내용과 저장된 내용을 비교한다.

연결 도우미에서는 `.pa-session/asset-reviews.json`에 검수 원장과 변경 이력을 저장한다. 이 파일은 Git과 공개 배포에서 제외한다. GitHub Pages에서는 같은 사이트·브라우저의 저장 공간을 사용하며 다른 기기와 자동 공유하지 않는다. 다른 기기로 옮길 때는 **검수 파일 저장/가져오기**를 사용한다. 공개 사이트에서 연결 도우미로 제작 구성을 넘길 때 검수 기록도 전달한다.

`review-catalog.json`은 원본 자산과 연결 규칙에서 빌드된다. `group-review-ledger.json`은 공개 배포할 검수 기록의 기본 원장이다. 검수 파일을 공개 원장에 반영하는 관리 명령은 `apply_group_reviews.py`가 제공한다.

## 검증

- `python prepare_pages.py --build`: 회귀검증 90개 통과.
- 신규 회귀검증: 모든 제작 박스의 검수 경로, 승인 범위와 출처, 수정·보류·분리, 관련 자산 변경, 영구 저장과 재시작, 동시 저장 충돌, 서버 인증과 다른 출처의 요청 차단, 변조·오래된 검수 내용의 제작 요청 차단, 모델에 전달되는 정리문.
- 별도 임시 원장과 8991 포트에서 승인 → 제작 박스 확인 → 검수 재료 필터 → 재료 찾기 → 보류 흐름을 확인했다. 실제 검수 원장에는 테스트 승인을 넣지 않았다.
- 모델 호출은 모의 호출로 전달 내용을 검사했다. 이 작업으로 새로운 문항을 생성하거나 수학적 품질을 승인하지 않았다.

UI 검증용 임시 파일은 실제 원장과 분리돼 있으며, 삭제 명령이 자동 승인 검토에서 정책상 차단되어 남겨 두었다. 검증 서비스는 종료한다. 임시 파일과 검수 내용은 공개 배포에 포함하지 않는다.
