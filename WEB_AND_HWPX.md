# 구독·AI 웹 제작과 한글 출력

## 현재 제공하는 경로

| 방식 | 사용자가 하는 일 | 과금·제한 |
|---|---|---|
| Codex 구독 연결 | 로그인된 PC 도우미에서 제작 버튼 클릭 | ChatGPT의 Codex 이용 한도 사용, Sol 고정 |
| Gemini·ChatGPT·Claude 웹 | 요청 복사 → AI 웹에 붙여넣기 → JSON 답변 가져오기 | 사용 중인 웹 계정의 무료·구독 한도 적용, 이 프로그램은 API를 호출하지 않음 |

웹 방식은 자동 연결이 아니다. 타 사이트의 로그인 세션이나 쿠키를 읽지 않는다. 각 웹의 무료 범위·사용량·모델 지원은 공급자와 계정에 따라 달라진다. 모든 AI 구독을 공통 API처럼 사용할 수 있다고 가정하지 않는다.

Google 계정의 공식 자동 연결 후보는 Gemini CLI다. 공식 문서는 개인 무료·유료 계정의 Google 로그인과, 저장된 인증을 사용하는 headless 실행을 안내한다. 이 릴리스에는 Gemini CLI 자동 실행·로그인을 구현하지 않았으며, 사용자의 Gemini 계정으로 실제 모델 응답을 받은 것으로 보고하지 않는다. 실제 Google 계정 연결이 필요한 다음 개발 경로다.

근거: [Codex 인증](https://learn.chatgpt.com/docs/auth), [Gemini CLI 인증](https://geminicli.com/docs/get-started/authentication/), [Gemini 웹 한도](https://support.google.com/gemini/answer/16275805?hl=en). 확인일 2026-09-21.

## 웹에서 주고받기

1. 풀이 지도에서 핵심과 마지막 질문까지 연결한다.
2. 제작 설정의 `사용 중인 AI 웹`을 선택하고 Gemini·ChatGPT·Claude 중 사용 중인 웹을 고른다.
3. `현재 구성으로 요청 새로 만들기` → `제작 요청 복사`를 누른다. 선택한 재료의 적용 조건·연결·역할·난도 주문·결과 형식을 담는다. 현재 구성에서 파생된 별도 요청이며, 준비 후 지도를 바꿨다면 새로 만들어야 한다.
4. 웹 대화에 붙여 넣고 JSON 답변을 받는다. JSON 전체 또는 JSON 코드 블록 하나를 가져온다. 손상된 역슬래시나 누락된 값을 추정해서 고치지 않는다.
5. `답변 검사하고 문항 보기`로 요청 번호·자산 판본·연결·누락 조건을 검사한다. PC 도우미에서는 통과 결과를 개인 제작 기록에 저장한다. 기존 기록은 덮어쓰지 않는다.

준비된 웹 요청과 출력 목록은 이 브라우저에 보관한다. 로그인 정보는 저장하지 않는다. 브라우저를 옮길 때는 요청 또는 출력 묶음 JSON을 사용한다. 공개 사이트와 PC 도우미는 서로 다른 출처이므로 저장 공간도 다르다.

## 한글 파일 출력

결과에서 `한글 출력에 담기`를 누른다. 최대 20문항을 담아 `한글 파일 받기 · 해설 미주 포함`으로 내려받는다. 각 항목의 `검토`로 원래 문항을 다시 읽고, `빼기`로 출력 목록에서 제외한다. 파일의 번호는 담은 순서다.

- 형식: 한글에서 편집 가능한 `.hwpx`. 옛 바이너리 `.hwp` 형식으로 가장하지 않는다.
- 본문: 2단 배치, 다음 문항은 다음 단에서 시작, 분석용 제목·원자 ID·난도 라벨은 제외한다.
- 번호: 문항마다 실제 한글 미주 참조 번호를 사용한다. 별도 수동 번호를 중복 인쇄하지 않는다.
- 해설: 새 쪽에서 시작하는 문서 끝 미주. 첫 문단은 `[정답]`, 이후 완전한 풀이를 넣는다.
- 수식: 이미지가 아닌 네이티브 한글 수식 개체. 분수·부등식·조합·괄호를 설치된 공통 수학 조판기로 변환한다.
- 지원: 텍스트·수식 문항. 그림·외부 명령·손상된 수식은 조용히 누락하지 않고 실패를 알린다. 복잡한 보기표·그림의 자동 편집 조판은 별도 범위다.

파일을 내려받아도 독립 검산·사람 승인 상태는 바뀌지 않는다. 서버도 출력 직전에 결과를 다시 검사하며 클라이언트가 보낸 통과 표시는 믿지 않는다.

한글 출력은 현재 PC 연결 도우미가 필요하다. 공개 화면에서는 출력 묶음을 JSON으로 저장한 뒤 도우미에서 가져온다. 생성 파일·입력은 임시 폴더에서 처리하고 응답 후 정리한다. 개인 결과와 출력 파일은 Pages에 게시하지 않는다.

## 설치 의존성

`server/export-hwpx.cjs`는 `PA_PYTHON` 또는 Codex 번들 Python을 사용한다. 수학 작성기는 `PA_MATH_SKILLS` 또는 사용자 홈의 `.codex/skills` 아래 `math-ocr-hwpx`, `korean-hwp-math-typesetting`을 읽는다. 이 개인 작성기 원본은 공개 저장소에 복제하지 않는다. 다른 PC에서는 해당 작성기를 설치하고 경로를 지정해야 한다. HWPX 생성 자체에는 한글 설치가 필요하지 않다. 실제 지면 검증에는 한글이 필요하다.

## 검증 범위

웹 JSON 파싱·선택한 재료 보존·오래된 요청 거절·위조 승인 무시·인증·출처 검사·중복 기록 방지·출력 범위를 회귀검증한다. 수학 작성기가 설치된 PC에서는 네이티브 미주 개수·번호·수식·다단·새 쪽·손상 수식 거절을 추가 검사한다. 작성기가 없는 CI에서는 이 네 검사를 생략한다고 명시하고 통과 수에 포함하지 않는다.

실제 Sol 제작 두 문항을 HWPX로 내보내고 한글 자체 PDF 렌더에서 문제와 새 쪽의 해설 미주, 분수·근호·수식·번호를 확인했다. 이는 해당 문항의 조판 확인이며 모든 생성 문항의 지면을 자동 승인하는 의미는 아니다.

실제 지면에서 발견한 축약 분수 `\frac a2`와 간격 명령 `\,`의 오변환을 어댑터에서 수정했다. 각각 명시적 분수 인자로 정규화하고 간격을 쉼표 목록과 분리한다. 이 관찰 사례를 회귀검증에 추가했으며 수정 후 한글 렌더를 다시 확인했다.
