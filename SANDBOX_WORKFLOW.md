# 추천 묶음 검수와 조합 결과

승격 대기는 먼저 9개 추론 단위로 39개 후보를 비교한다. 이 묶음은 검토 단위이며 구성원 전체를 한 개념으로 합친다는 뜻이 아니다. 정리문을 승인해도 일반화 오류가 남은 옛 정의를 자동 승인하지 않는다.

## 사람 검토

1. 강사 이름을 입력한다. 추천 묶음의 정리문, 전제, 원래 항목을 비교한다.
2. 일부 체크박스를 선택하고 **선택 항목 분리**를 누르면 선택한 항목들이 별도 묶음으로 나온다. 분리 후 양쪽 정리문을 고쳐서 각각 승인하거나 보류한다.
3. **묶음 검토 보내기**로 JSON을 저장한다. 기록은 이름별로 브라우저에 저장되며 새로고침 뒤에도 유지된다. 자동 실시간 공유는 아니다.
4. 운영자는 `python apply_group_reviews.py 검토파일.json --dry-run`으로 검증 후 같은 명령에서 `--dry-run`을 빼서 반영한다. `묶음검토_반영.cmd`에 파일을 전달해도 된다.
5. `group-review-ledger.json`을 배포하면 다른 기기에서도 해당 이름의 검토 결과를 읽을 수 있다. 이후 기존 `tools/apply_promotion_decisions.py`의 refine/merge와 교차검토·ratify 절차에서 정리문과 분리 결과를 사용한다. 묶음 검토 원장이 공유 정본을 자동 생성하지는 않는다.

원문·적용 전제·추천 내용이 달라지면 revision이 달라진다. 오래된 검토 파일은 반영을 거절하고 다시 검토한다. 분리 결과의 누락·중복·외부 ID·다른 검토자·더 오래된 결정도 거절한다. 기존 후보·댓글·승격 원장은 삭제하지 않는다.

## 실험 문항

조합 예시는 세 생성 가족에서 각각 24개, 총 72개를 생성한다.

- 차형 적분 미분 + 적분 부호 + 영점 충돌 (+ 부등식 후보 선택)
- 상수항 소거 + 붙임점 미분 + 절댓값 부호별 적분
- 외부 함수의 근으로 합성 분해 + 중복도 계산 + 절댓값 미분 판정

화면의 랜덤 버튼은 이 검산된 은행에서 선택한다. 자유로운 모든 원자의 순열을 즉석에서 생성하는 엔진은 아니다. 원문과 동일한 표현을 붙이는 대신 해당 추론의 적용 전제를 새 문제에서 다시 확인한다. 표시된 요소 이름은 원본 자산으로 연결된다. 구조별 연결은 전이 사례이며 원본 문제 전체의 복제나 원본 레시피 계약 전체의 충족을 주장하지 않는다.

같은 번호로 재현하거나 문제·정답·풀이·사용 자산·검증 정보를 JSON으로 저장할 수 있다. 새 자산 버전에서는 번호와 결과 은행이 바뀌므로 이전 결과를 보존하려면 저장 파일을 사용한다. 성립 검산은 고난도·참신성·출제 품질의 보증이 아니다. 현재 승인 공유자산이 0개이므로 후보를 사용한 실험으로 표시한다.

## 자산 추가 시 갱신

`build_asset_library.py`는 공유 데이터 생성 후 실험 은행도 재생성한다. 배포의 `prepare_pages.py --build`는 세 JSON 데이터와 연결 설정을 읽어 추천 묶음과 은행을 갱신하고 SymPy 독립 검사를 통과해야 배포한다. 신규 후보는 누락시키지 않고 개별 검토 항목으로 표시한다. 문구 유사도만으로 기존 묶음에 자동 병합하지 않는다.

신규 승인 자산이 기존 생성 역할을 충족하는지 확인한 뒤 `combination-bindings.json`에 `{asset_id, source_motif_id, asset_revision}`를 등록한다. asset_revision은 `build_sandbox.asset_revision(asset)` 값이며 contentHash가 있으면 그 값을 쓴다. 대응하는 기존 motif 역할의 전제·결론이 새 자산으로 충족되는지 운영자가 검토한 연결만 등록한다. 이후 새 자산도 그 역할의 선택지로 자동 편입된다. 해시가 달라지면 연결을 사용하지 않고 **내용 변경으로 연결 재검토**에 표시한다. 생성 역할이 없는 자산은 **아직 연결되지 않은 요소**에 남고 추가 생성 규칙이 필요하다.

## 검증

`python -m pip install sympy==1.14.0` 후 `python -m unittest test_sandbox -v`.

72개 모두를 생성 코드와 별도로 미분·인수분해·실근 격리·정적분으로 검사한다. 추가로 전체 후보의 중복 없는 포함, 신규 유입, 분리 유효성, 오래된 결정 거절, 같은 번호 재현, 필요 요소 누락, 승인 자산 편입과 내용 변경 시 제외를 검사한다. 임시 추가 자산은 TemporaryDirectory 내부에서만 만들며 종료 시 삭제한다. 검증 파일과 실제 실험 결과 은행은 유지한다.
