# 실시간 문항 선점 설정

현재 GitHub Pages는 읽기 전용 정적 호스팅이다. 구성원의 선점 이름을 즉시 공유하려면 Problem Atom 전용 Supabase 프로젝트를 한 번 연결해야 한다. NGD2 Supabase 프로젝트와 데이터·계정·테이블을 공유하지 않는다.

## 최초 1회

1. Supabase에서 새 프로젝트를 만든다.
2. Authentication → Providers에서 Anonymous Sign-Ins를 활성화한다.
3. SQL Editor에서 `realtime/supabase_setup.sql`을 실행한다.
4. Project Settings → API에서 Project URL과 **Publishable key**를 확인한다.
5. `realtime-config.js`를 아래처럼 바꾼다.

```javascript
window.PA_REALTIME_CONFIG = {
  enabled: true,
  url: "https://프로젝트.supabase.co",
  publishableKey: "sb_publishable_...",
};
```

6. 커밋·푸시한다. 이후 구성원이 현황판에 처음 접속하면 익명 사용자 UUID가 자동 발급되고 브라우저에 로그인 세션이 유지된다.

## 보안 경계

- `sb_secret_...`, `service_role`, DB 비밀번호는 웹 파일에 절대 넣지 않는다.
- 브라우저에는 Publishable key만 둔다.
- RLS가 본인 프로필과 본인이 선점한 행만 수정·삭제하도록 제한한다.
- 문항 ID가 기본키이므로 두 사람이 동시에 선점하면 DB가 한 명만 승인한다.
- 선점 상태는 `pa_question_claims` 변경 구독으로 다른 화면에 즉시 전달된다.

## 운영

- 이름은 1~20자로 입력한다.
- 같은 기기에서는 같은 익명 멤버 ID를 유지한다.
- 기기를 바꾸면 새 멤버 ID가 생기므로, 기존 기기에서 선점을 취소한 뒤 옮기는 것을 원칙으로 한다.
- 구성원의 실명 대신 프로젝트용 표시 이름을 써도 된다.

참고: [Supabase 익명 로그인](https://supabase.com/docs/reference/javascript/auth-signinanonymously), [Postgres Changes](https://supabase.com/docs/guides/realtime/postgres-changes)

